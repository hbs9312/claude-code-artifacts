/**
 * Comments module - provides comment threading and UI support
 */

export { CommentController } from './CommentController';
export {
  renderComment,
  renderCommentThread,
  renderCommentStats,
  renderCommentsSection,
  renderInlineCommentButton,
  getCommentStyles,
} from './CommentThread';
